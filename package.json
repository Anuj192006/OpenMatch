{
  "name": "openmatch-ai",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && python3 -m pip install -r backend/requirements.txt",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"python3 -m uvicorn backend.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
