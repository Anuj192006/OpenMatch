services:
  - type: web
    name: openmatch-ai-backend
    runtime: python
    buildCommand: python3 -m pip install -r backend/requirements.txt
    startCommand: python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
